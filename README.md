# DeskRobot

## build

```bash
# clone the repo
git clone <repo>
# enter the repo
cd <repo>
# sync uv environment
uv sync --dev

# run
uv run python scripts/main.py

# or
source .venv/bin/activate
python scripts/main.py
```