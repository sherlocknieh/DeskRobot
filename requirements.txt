# 车轮电机, 舵机, OLED屏幕, LED灯, 温湿度传感器
gpiozero; sys_platform == 'linux'
pigpio; sys_platform == 'linux'
lgpio; sys_platform == 'linux'
# sudo systemctl start pigpiod
# sudo systemctl enable pigpiod


# OLED显示
pillow
luma.oled; sys_platform == 'linux'

# 手柄控制
evdev; sys_platform == 'linux'

# 音乐播放
pygame

# 温湿度传感器
adafruit-circuitpython-dht


# 摄像头
picamera2; sys_platform == 'linux'
opencv-python
flask
# 人脸跟踪
mediapipe==0.10.9
simple_pid


# 语音相关
pyaudio
torch
torchaudio
pydub
edge-tts  
websocket # 讯飞stt用到
requests  # siliconflow stt用到
# 语音唤醒
numpy==1.26.4  #pip install --upgrade "numpy<2"
openwakeword
speexdsp-ns  # apt-get install libspeex-dev libspeexdsp-dev


# AI相关
langchain
langchain-openai
langgraph
